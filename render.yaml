services:
  - type: web
    name: tts-server
    runtime: node
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production